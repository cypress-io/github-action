{
  "name": "example-config",
  "version": "1.0.0",
  "description": "pass baseUrl config value via action",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "build": "echo building ... server ... done!",
    "start": "serve public --listen 3333",
    "cy:open": "cypress open"
  },
  "private": true,
  "devDependencies": {
    "cypress": "15.8.0",
    "serve": "14.2.5"
  }
}
