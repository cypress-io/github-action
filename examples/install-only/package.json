{
  "name": "example-install-only",
  "version": "2.0.0",
  "description": "Do not install _every_ dependency, just Cypress before running tests",
  "type": "module",
  "scripts": {
    "test": "cypress run"
  },
  "private": true,
  "dependencies": {
    "arg": "5.0.0",
    "debug": "4.3.6"
  },
  "devDependencies": {
    "cypress": "15.14.1"
  }
}
